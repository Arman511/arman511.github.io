{
  "name": "gh_pages",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist -b main -t true -e .gitignore"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.0.11"
  }
}
